// API Configuration
// Use environment variable for production, fallback to localhost for development
const API_BASE_URL = `${window.location.protocol}//${window.location.hostname}/autronicas-inventory-system/api`;


export default API_BASE_URL;

